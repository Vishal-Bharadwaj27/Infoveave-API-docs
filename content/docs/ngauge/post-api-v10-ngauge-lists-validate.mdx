---
title: "Validate List"
description: "Validates the list query/config before save. First API in this flow so a bad query never becomes a scheduled job."
---

## POST `/api/v10/ngauge/lists/validate`

**Validate List**

This endpoint exists to fail fast on a bad list definition. Validating before save or sync protects every form that depends on the list from inheriting broken configuration.

Tags: `NGague \ List`

### Parameters

```json
[]
```

### Request Body

```json
{
  "content": {
    "application/json-patch+json": {
      "schema": {
        "$ref": "#/components/schemas/ListConfigValidationRequest"
      }
    },
    "application/json": {
      "schema": {
        "$ref": "#/components/schemas/ListConfigValidationRequest"
      }
    },
    "text/json": {
      "schema": {
        "$ref": "#/components/schemas/ListConfigValidationRequest"
      }
    },
    "application/*+json": {
      "schema": {
        "$ref": "#/components/schemas/ListConfigValidationRequest"
      }
    }
  },
  "required": true
}
```

### Responses

```json
{
  "200": {
    "description": "OK"
  },
  "400": {
    "description": "Bad Request",
    "content": {
      "application/problem+json": {
        "schema": {
          "$ref": "#/components/schemas/ProblemDetails"
        }
      }
    }
  }
}
```
