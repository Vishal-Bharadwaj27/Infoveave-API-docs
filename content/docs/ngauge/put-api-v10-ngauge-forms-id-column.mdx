---
title: "Update Column"
description: "Updates an existing column mapping. Exists so you can rename/retype without dropping the column."
---

## PUT `/api/v10/ngauge/Forms/{id}/column`

**Update Column**

This endpoint exists to rename or reconfigure a field in place. In-place edits preserve the column's data while fixing labels, types or rules.

Tags: `NGague \ Form \ Column`

### Parameters

```json
[
  {
    "name": "id",
    "in": "path",
    "required": true,
    "schema": {
      "pattern": "^-?(?:0|[1-9]\\d*)$",
      "type": [
        "integer",
        "string"
      ],
      "format": "int64"
    }
  }
]
```

### Request Body

```json
{
  "content": {
    "application/json-patch+json": {
      "schema": {
        "$ref": "#/components/schemas/ColumnUpdateRequest"
      }
    },
    "application/json": {
      "schema": {
        "$ref": "#/components/schemas/ColumnUpdateRequest"
      }
    },
    "text/json": {
      "schema": {
        "$ref": "#/components/schemas/ColumnUpdateRequest"
      }
    },
    "application/*+json": {
      "schema": {
        "$ref": "#/components/schemas/ColumnUpdateRequest"
      }
    }
  },
  "required": true
}
```

### Responses

```json
{
  "200": {
    "description": "OK"
  },
  "400": {
    "description": "Bad Request",
    "content": {
      "application/problem+json": {
        "schema": {
          "$ref": "#/components/schemas/ProblemDetails"
        }
      }
    }
  }
}
```
