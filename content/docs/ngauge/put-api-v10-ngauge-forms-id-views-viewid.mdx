---
title: "Update View"
description: "Updates a view's layout JSON. Exists so layouts can change without touching form columns."
---

## PUT `/api/v10/ngauge/Forms/{id}/views/{viewId}`

**Update View**

This endpoint exists to refine a view as reporting needs change. Editable views stay relevant instead of accumulating stale duplicates.

Tags: `NGague \ Form \ View`

### Parameters

```json
[
  {
    "name": "id",
    "in": "path",
    "required": true,
    "schema": {
      "pattern": "^-?(?:0|[1-9]\\d*)$",
      "type": [
        "integer",
        "string"
      ],
      "format": "int64"
    }
  },
  {
    "name": "viewId",
    "in": "path",
    "required": true,
    "schema": {
      "pattern": "^-?(?:0|[1-9]\\d*)$",
      "type": [
        "integer",
        "string"
      ],
      "format": "int64"
    }
  }
]
```

### Request Body

```json
{
  "content": {
    "application/json-patch+json": {
      "schema": {
        "$ref": "#/components/schemas/NgaugeFormViewUpdateRequest"
      }
    },
    "application/json": {
      "schema": {
        "$ref": "#/components/schemas/NgaugeFormViewUpdateRequest"
      }
    },
    "text/json": {
      "schema": {
        "$ref": "#/components/schemas/NgaugeFormViewUpdateRequest"
      }
    },
    "application/*+json": {
      "schema": {
        "$ref": "#/components/schemas/NgaugeFormViewUpdateRequest"
      }
    }
  },
  "required": true
}
```

### Responses

```json
{
  "200": {
    "description": "OK"
  },
  "400": {
    "description": "Bad Request",
    "content": {
      "application/problem+json": {
        "schema": {
          "$ref": "#/components/schemas/ProblemDetails"
        }
      }
    }
  }
}
```
