---
title: "Create a connection"
description: "Validates the name, category, provider, auth mode, and settings against the provider catalogue, then creates a new tenant-scoped connection and returns its ID, public settings, revision, and metadata without exposing secrets."
---

{/* enum-hover */}

## POST `/api/v10/Connections/connections`

**Create a connection**

This endpoint exists to create a connection with fully validated settings in one step. Validating up front means downstream queries and jobs can resolve credentials securely instead of failing later on a half-configured connection.

Tags: `Connections`

### Parameters

```json
[]
```

### Request Body

```json
{
  "content": {
    "application/json-patch+json": {
      "schema": {
        "$ref": "#/components/schemas/CreateConnectionRequest"
      }
    },
    "application/json": {
      "schema": {
        "$ref": "#/components/schemas/CreateConnectionRequest"
      }
    },
    "text/json": {
      "schema": {
        "$ref": "#/components/schemas/CreateConnectionRequest"
      }
    },
    "application/*+json": {
      "schema": {
        "$ref": "#/components/schemas/CreateConnectionRequest"
      }
    }
  },
  "required": true
}
```

### Responses

```json
{
  "201": {
    "description": "Created",
    "content": {
      "text/plain": {
        "schema": {
          "$ref": "#/components/schemas/ConnectionSummary"
        }
      },
      "application/json": {
        "schema": {
          "$ref": "#/components/schemas/ConnectionSummary"
        }
      },
      "text/json": {
        "schema": {
          "$ref": "#/components/schemas/ConnectionSummary"
        }
      }
    }
  },
  "400": {
    "description": "Bad Request",
    "content": {
      "application/problem+json": {
        "schema": {
          "$ref": "#/components/schemas/ProblemDetails"
        }
      }
    }
  }
}
```
